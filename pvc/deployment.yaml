---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp-deployment
  namespace: test
  labels:
    tier: frontend
    app: nginx
spec:
  selector:
    matchLabels:
      app: myapp
  replicas: 3
  template:
    metadata:
      name: nginx
      namespace: test
      labels:
        app: myapp
    spec:
      containers:
        - name: nginx
          image: nginx

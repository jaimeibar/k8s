---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-deploy
  namespace: vote
  labels:
    name: redis-deploy
    app: demo-voting-app
spec:
  replicas: 1
  selector:
    matchLabels:
      name: redis-pod
      app: demo-voting-app
  template:
    metadata:
      name: redis-pod
      namespace: vote
      labels:
        name: redis-pod
        app: demo-voting-app
    spec:
      containers:
        - name: redis
          image: redis
          resources:
          ports:
            - containerPort: 6379
